#!/bin/sh
# OpenVCam hooks mediaserverd's capture graph (like the original vcamera).
# Restart mediaserverd so the tweak loads; it respawns automatically.
killall -9 mediaserverd 2>/dev/null || true
exit 0
